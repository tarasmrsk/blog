export const selectLoading = (state) => state.id.loading
export const selectCurrentSlug = (state) => state.id.currentSlug
export const selectCurrentPage = (state) => state.id.currentPage
export const selectArticles = (state) => state.id.articles
export const selectTotalArticles = (state) => state.id.total

export const selectIsAuthenticated = (state) => state.login.isAuthenticated

